[package]
name = "mev-relayer-protos"
version = "0.2.0"
edition = "2024"

[dependencies]
tonic = "*"
prost = "*"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time"] }
tokio-stream = "0.1"


[build-dependencies]
tonic-build = "*"
